# Cleanup Tasks

The following files/directories have been marked for cleanup:

- `temp_cleanup/`: Contains prototype scripts and temporary test files that should be deleted once the core functionality is fully verified.
    - `test_entry.py`
    - `test_typer.py`
